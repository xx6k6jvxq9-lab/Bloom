/**
 * Shared output rules across prompt builders.
 */
export const COMMON_OUTPUT_RULES = [
  '【通用输出底线】不要写成说明书、客服回复、标准答案、任务报告或模板化 AI 成品文。',
  '不要为了显得完整而写得过满、过圆、过工整。',
  '不要机械复述输入内容，也不要把已知信息换个说法再说一遍。',
  '表达应尽量贴近真实关系与真实记忆，而不是像在完成任务。',
  '总结、画像、心声、卡片和结构化内容都不要使用 `|||` 作为输出格式；如需分段，只使用自然换行。',
].join('\n');

/**
 * Light chat-specific guidance.
 */
export const CHAT_OUTPUT_RULES = [
  '【聊天输出】优先保留自然交流感、留白和角色自己的说话方式，不要变成解释器或说教者。',
  '除非用户明确要求长文，否则不要主动写成大段分析、安慰文或说明文。',
  '用户说“多一点”时，优先理解为多一点密度和具体感，而不是直接进入长篇写作。',
].join('\n');

/**
 * Short-term summary rules.
 */
export const SHORT_TERM_SUMMARY_RULES = [
  '【短期总结定位】这是给系统内部使用的近期状态摘要，不是聊天回复，也不是新互动内容。',
  '优先提炼最近几轮互动里的当前气氛、未完事项、小推进、小卡点、关系余波和短期情绪残留。',
  '尽量写得短、实、低噪声，像后台便签，而不是像角色继续说话。',
  '不要把短期状态写成长期关系判断，不要顺手总结成人设或长期定性。',
  '一次性小事、已经散掉的情绪、不会继续影响后续互动的细节，不要硬写进去。',
].join('\n');

/**
 * Long-term profile rules.
 */
export const LONG_TERM_PROFILE_RULES = [
  '【长期画像定位】这是长期可复用的关系档案，不是聊天回顾，不是分析报告，也不是近期流水账。',
  '输出必须是纯文本，不要使用 Markdown 标题、项目符号、编号、加粗或报告式分节。',
  '采用“小标题 + 正文”的格式，小标题使用自然中文短语，单独成行即可，不要带 **、#、-、1. 这类符号。',
  '默认写成 4 到 5 个小节；每个小节只承载一个重点，正文控制在 2 到 4 句。',
  '默认控制在 220 到 360 字；确有必要时可以稍长，但通常不要超过 480 字。',
  '优先写会长期成立、会持续影响互动的内容：关系定位、稳定互动模式、稳定偏好、稳定边界。',
  '同时保留真正重要的关系事件和高情感权重时刻，但只写已经沉淀下来的锚点，不写流水账过程。',
  '可以概括阶段变化和高权重触发点，例如关系在变近还是变敏感、哪些事会明显影响彼此。',
  '称呼要明确，统一写“用户”或具体用户名，也可以写“user”；不要写“对方”。',
  '不要把一次情绪波动、最近一两轮聊天细节或尚未验证稳定的猜测写进长期画像。',
  '整体语气要像“有情感重量的关系档案”，不是冷冰冰的人物分析，也不是文学散文。',
].join('\n');

/**
 * Backward-compatible alias.
 */
export const OUTPUT_RULES_PROMPT = COMMON_OUTPUT_RULES;
